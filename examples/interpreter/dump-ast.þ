-- Dump the parsed program on stdin: a parser smoke test.
import core, list, math, text, parse in

let
	render = [tag, a, b] -> if (eq tag parse.resultOk)
		(format "ok: %v" [a;])
		(format "parse error: %s at offset %v" [a; b])
in
	parse.parseProgram stdin > render > write
